instances:
  alpha:
    bundle: foundation
    working_dir: /mnt/c/Users/charl/OneDrive/Amplifier Data
    persona:
      name: Director
      emoji: ":robot_face:"
    a2a:
      port: 8223
      realtime_response: true
      agent_name: The Director
      agent_description: "Charlie's personal AI orchestrator via Slack"
  
  beta:
    bundle: foundation
    working_dir: ~/amplifier-working-beta
    persona:
      name: Beta
      emoji: ":gear:"

defaults:
  instance: alpha

slack:
  app_token: ${SLACK_APP_TOKEN}
  bot_token: ${SLACK_BOT_TOKEN}
