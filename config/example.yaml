instance:
  name: alpha
  bundle: foundation
  working_dir: ~/amplifier-working
  persona:
    name: Alpha
    emoji: ":robot_face:"

slack:
  app_token: ${SLACK_APP_TOKEN}
  bot_token: ${SLACK_BOT_TOKEN}
